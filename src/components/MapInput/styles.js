import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  view: {
    flex: 1,
    width: '100%',
    height: 300
  },
  map: {
    width: '100%',
    height: '100%'
  }
});

export default styles;
